import { MCP_CLIENT_ICONS } from '@/app/(app)/[locale]/(landing-v2)/v2/lib/icons';

/** Clients shown on the compatibility wall. Any MCP client works; these are the ones with a recognisable mark. */
export const MCP_CLIENTS = MCP_CLIENT_ICONS;
