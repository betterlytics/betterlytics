import { CUSTOMER_ICONS, CUSTOMER_ICONS_MORE } from '@/app/(app)/[locale]/(landing-v2)/v2/lib/icons';

/**
 * Placeholder logo wall carried over from the draft; these are not customers. Replace before launch.
 * The first eight fill the board; anything beyond that is what the board flips in over time.
 */
export const CUSTOMERS = [...CUSTOMER_ICONS, ...CUSTOMER_ICONS_MORE];
