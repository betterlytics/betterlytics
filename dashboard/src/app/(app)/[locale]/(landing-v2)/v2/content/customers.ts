import { CUSTOMER_ICONS } from '@/app/(app)/[locale]/(landing-v2)/v2/lib/icons';

/** Placeholder logo wall carried over from the draft; these are not customers. Replace before launch. */
export const CUSTOMERS = CUSTOMER_ICONS;
