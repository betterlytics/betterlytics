import { mockGeographyData } from '@/presenters/toWorldMap';

/** Contains SSR safe obscured visitor data for all countries of the world, which we have a FlagIcon countryCode for */
export const MOCK_WORLD_GEOVISITORS = mockGeographyData();
